----------------

(UTF-8)

這個目錄包含幾個 Java 範例檔。您至少必須修改檔案內的使用者名稱、密碼
(conn = DriverManager.getConnection ... 這一行)，之後再以 JDK 1.3 版以上
編譯才行。

Java 原始檔全都以 UTF-8 字集儲存，因此編譯的時候請加上 -encoding utf8，如:

> javac -encoding utf8 SampleIntro.java

其它介紹請參考第十七章。

---------------

(c) 2005 Michael Kofler

